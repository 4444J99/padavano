{
  "name": "padavano",
  "version": "1.0.0",
  "description": "Systems Architecture & Product Engineering — Padavano consulting site",
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "http-server": "14.1.1",
    "postcss-html": "1.8.1",
    "stylelint": "17.14.0",
    "stylelint-config-standard": "40.0.0"
  },
  "scripts": {
    "dev": "http-server src -p 8080 -o",
    "lint": "stylelint 'src/**/*.html' --config .config/stylelint.config.mjs",
    "test": "playwright test --config .config/playwright.config.ts",
    "validate": "npm run lint && npm test"
  }
}
